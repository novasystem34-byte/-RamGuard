<Application x:Class="RamGuard.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:RamGuard"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <local:PercentToWidthConverter x:Key="PercentToWidth"/>
            <local:BoolToVisibilityConverter x:Key="BoolToVis"/>
            <local:BoolToExpandArrowConverter x:Key="ExpandArrow"/>

            <!-- ألوان الثيم الداكن -->
            <SolidColorBrush x:Key="BgDark" Color="#0F1115"/>
            <SolidColorBrush x:Key="BgPanel" Color="#171A21"/>
            <SolidColorBrush x:Key="BgRow" Color="#1E222B"/>
            <SolidColorBrush x:Key="BorderColor" Color="#2A2F3A"/>
            <SolidColorBrush x:Key="TextPrimary" Color="#E8EAF0"/>
            <SolidColorBrush x:Key="TextSecondary" Color="#8A90A2"/>
            <SolidColorBrush x:Key="Accent" Color="#5B8CFF"/>
            <SolidColorBrush x:Key="Warning" Color="#FFB454"/>
            <SolidColorBrush x:Key="Danger" Color="#FF5D5D"/>

            <Style TargetType="Button" x:Key="FlatButton">
                <Setter Property="Background" Value="{StaticResource BgRow}"/>
                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="10,5"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="5">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="6,2"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{StaticResource Accent}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style TargetType="TextBox" x:Key="FlatTextBox">
                <Setter Property="Background" Value="{StaticResource BgRow}"/>
                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="6,4"/>
                <Setter Property="CaretBrush" Value="{StaticResource TextPrimary}"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
